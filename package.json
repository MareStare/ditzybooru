{
  "name": "ditzybooru",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "intent": {
    "skills": [
      "@tanstack/*"
    ]
  },
  "engines": {
    "node": "^24"
  },
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "preview": "npm run preview --workspace frontend",
    "type-check": "npm run type-check --workspace frontend",
    "test": "npm run test --workspace frontend",
    "check:a11y": "npm run check:a11y --workspace frontend",
    "lint": "eslint",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "bootstrap-claude": "./scripts/bootstrap-claude.sh"
  },
  "devDependencies": {
    "typescript": "^6.0",
    "prettier": "^3.8",
    "eslint": "^10.5",
    "typescript-eslint": "^8.61",
    "@tanstack/eslint-config": "^0.4"
  }
}
